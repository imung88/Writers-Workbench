{
  "name": "writers-workbench-mcp",
  "version": "1.1.0",
  "private": true,
  "description": "MCP server + localhost bridge that lets an AI read and edit Writer's Workbench projects while the offline HTML app stays open.",
  "type": "module",
  "bin": {
    "writers-workbench-mcp": "server.js"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node test-classify.mjs && node test-e2e.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "engines": {
    "node": ">=18"
  }
}
